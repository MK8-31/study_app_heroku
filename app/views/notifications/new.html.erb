<% provide(:title,"予定を追加") %>

<% if @homework_test == "true" %>
    <h1>テスト予定追加</h1>
<% else %>
    <h1>課題予定追加</h1>
<% end %>


<div class="row">
    <div class="col-md-6 col-md-offset-3 wht">
        <% if @subject.nil? %>
            <p>「<%= link_to '時間割を見る',subjects_path %>」からでも追加できます。</p>
        <% end %>
        <%= form_with(model: @notice,local: true) do |f| %>
            <%= render 'shared/error_messages',object: f.object %>
            
            <b>予定名</b>

            <%= f.text_field :subject,:value => @subject %>
            
            <b>予定日</b>
            <%= f.datetime_field :start_time %>
            <p>設定された予定日の前日にメールでお知らせします。</p>
            
            <%= f.hidden_field :homework_test,:value => @homework_test %>
            
            <%= f.hidden_field :over, :value => false %>
            
            <%= f.submit "追加",class: "btn btn-primary" %>
        <% end %>
            
    </div>
</div>