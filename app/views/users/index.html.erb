<% provide(:title,'All users') %>
<h1>All users</h1>

<div class="row">
    <div class="col wht">
        <%= will_paginate %>

        <ul class="users">
            <% @users.each do |user| %>
                <%= render user %>
            <% end %>
        </ul>
        
        <%= will_paginate %>  
    </div>
</div>
